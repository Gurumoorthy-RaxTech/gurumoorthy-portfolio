# Build output
bin/
obj/

# Local secrets - never commit real connection strings/passwords.
# Each project's appsettings.json has a placeholder; put your real local
# value in appsettings.Development.json (already gitignored below) or set
# the environment variable documented in that project's README.
**/appsettings.Development.json
**/appsettings.Local.json
*.local.json

# Local SQLite databases generated by running the EF Core Code-First demo
*.db
*.db-shm
*.db-wal

# Visual Studio / Rider / VS Code
.vs/
.vscode/
*.user
*.suo
